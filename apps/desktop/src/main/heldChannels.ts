const EVERYTHING_HELD = 'valence.held.all';

const KEEP_ONE = 'valence.held.keep';

const DROP_ONE = 'valence.held.drop';

const PAUSE_ONE = 'valence.held.pause';

const WHAT_CHANGED = 'valence.held.changed';

const CAN_REACH_NOW = 'valence.reach.now';

const REACH_CHANGED = 'valence.reach.changed';

export {
  CAN_REACH_NOW,
  DROP_ONE,
  EVERYTHING_HELD,
  PAUSE_ONE,
  KEEP_ONE,
  REACH_CHANGED,
  WHAT_CHANGED,
};

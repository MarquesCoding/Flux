const READ_EVERYTHING = 'valence.preferences.all';

const WRITE_ONE = 'valence.preferences.write';

const FORGET_ONE = 'valence.preferences.forget';

const GO_TO_THE_SERVER = 'valence.goToTheServer';

const CHANGE_SERVER = 'valence.changeServer';

const NOW_WATCHING = 'valence.nowWatching';

export { CHANGE_SERVER, FORGET_ONE, GO_TO_THE_SERVER, NOW_WATCHING, READ_EVERYTHING, WRITE_ONE };

const READ_EVERYTHING = 'flux.preferences.all';

const WRITE_ONE = 'flux.preferences.write';

const FORGET_ONE = 'flux.preferences.forget';

const GO_TO_THE_SERVER = 'flux.goToTheServer';

export { FORGET_ONE, GO_TO_THE_SERVER, READ_EVERYTHING, WRITE_ONE };

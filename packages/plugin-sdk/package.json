{
  "name": "@flux/plugin-sdk",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc --build",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@flux/contracts": "workspace:*",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "vite-tsconfig-paths": "^5.1.4",
    "vitest": "^3.0.2"
  }
}

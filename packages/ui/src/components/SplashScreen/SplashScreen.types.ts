type SplashScreenProps = {
  /**
   * The name shown while loading, which an operator may have renamed.
   */
  name?: string;
  /**
   * What is being waited for, for anyone who cannot see the bar.
   */
  label?: string;
};

export type { SplashScreenProps };

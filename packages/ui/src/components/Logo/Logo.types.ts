type LogoProps = {
  size?: number;
  isDotted?: boolean;
  hasEdge?: boolean;
  isAnimated?: boolean;
  isSolid?: boolean;
  label?: string;
  className?: string;
  src?: string;
};

export type { LogoProps };

type SkeletonProps = {
  /**
   * What is being waited for, so a screen reader is told something is coming
   * rather than meeting a row of empty boxes.
   */
  label?: string;
  className?: string;
};

export type { SkeletonProps };
